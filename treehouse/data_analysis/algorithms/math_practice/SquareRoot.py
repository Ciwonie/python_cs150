# This algorithm is designed to guess the square root of any number
# First, enter the number tou want rooted
# Secondanly, input your guess of the first number's root

import os


def clear_screen():
    os.system("cls" if os.name == "nt" else "clear")
